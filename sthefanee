<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Remote Sell Essence | AI Prompt Generator</title>
  <style>
    :root {
      --primary: #7b5cf3;
      --secondary: #f8b6e0;
      --bg: #fdfbff;
      --text-dark: #2b2b2b;
      --text-light: #555;
    }

    body {
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, var(--secondary) 0%, var(--primary) 100%);
      margin: 0;
      padding: 2rem;
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      color: var(--text-dark);
    }

    .container {
      background: var(--bg);
      max-width: 650px;
      width: 100%;
      border-radius: 16px;
      box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
      padding: 2.5rem;
      animation: fadeIn 0.8s ease;
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }

    h1 {
      text-align: center;
      color: var(--primary);
      margin-bottom: 1rem;
    }

    p.subtext {
      text-align: center;
      color: var(--text-light);
      margin-bottom: 2rem;
    }

    label {
      font-weight: 600;
      display: block;
      margin-top: 1rem;
    }

    input, select {
      width: 100%;
      padding: 0.8rem;
      border: 1px solid #ccc;
      border-radius: 10px;
      margin-top: 0.5rem;
      font-size: 0.95rem;
    }

    button {
      width: 100%;
      margin-top: 1.5rem;
      padding: 1rem;
      font-size: 1rem;
      background: var(--primary);
      color: white;
      border: none;
      border-radius: 10px;
      cursor: pointer;
      transition: background 0.3s;
    }

    button:hover {
      background: #6c4be0;
    }

    .output {
      margin-top: 2rem;
      background: #f8f5ff;
      border: 1px solid #e3d9fa;
      border-radius: 12px;
      padding: 1.2rem;
      position: relative;
    }

    .output h3 {
      margin-top: 0;
      color: var(--primary);
    }

    #finalPrompt {
      white-space: pre-line;
      margin-top: 0.5rem;
      font-size: 1rem;
      color: var(--text-dark);
    }

    .tip {
      color: var(--text-light);
      font-style: italic;
      margin-top: 0.5rem;
    }

    .copy-btn {
      position: absolute;
      top: 15px;
      right: 15px;
      background: var(--secondary);
      border: none;
      border-radius: 8px;
      padding: 0.5rem 0.8rem;
      cursor: pointer;
      color: #333;
      font-size: 0.85rem;
      transition: background 0.3s;
    }

    .copy-btn:hover {
      background: #f59dd5;
    }

    .footer {
      text-align: center;
      margin-top: 1.5rem;
      font-size: 0.85rem;
      color: var(--text-light);
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>✨ AI Prompt Generator</h1>
    <p class="subtext">Created by <strong>Remote Sell Essence</strong> to craft perfect prompts for any purpose</p>

    <label>🎯 What’s your goal or topic?</label>
    <input type="text" id="goal" placeholder="e.g., create a social media calendar">

    <label>👩‍💼 Choose the AI role</label>
    <select id="role">
      <option value="expert marketer">Expert marketer</option>
      <option value="content writer">Content writer</option>
      <option value="teacher or tutor">Teacher / Tutor</option>
      <option value="developer or coder">Developer / Coder</option>
      <option value="business strategist">Business strategist</option>
      <option value="creative storyteller">Creative storyteller</option>
      <option value="custom">Custom (type below)</option>
    </select>

    <input type="text" id="customRole" placeholder="If custom, type your own role">

    <label>🗣️ Tone or Style</label>
    <select id="tone">
      <option value="professional">Professional</option>
      <option value="friendly">Friendly</option>
      <option value="persuasive">Persuasive</option>
      <option value="educational">Educational</option>
      <option value="storytelling">Storytelling</option>
    </select>

    <label>📄 Output Format</label>
    <select id="format">
      <option value="text paragraph">Text paragraph</option>
      <option value="list or outline">List / Outline</option>
      <option value="script or dialogue">Script / Dialogue</option>
      <option value="step-by-step plan">Step-by-step plan</option>
      <option value="template or framework">Template / Framework</option>
    </select>

    <button onclick="generatePrompt()">✨ Generate Prompt</button>

    <div class="output" id="output" style="display:none;">
      <button class="copy-btn" onclick="copyPrompt()">Copy</button>
      <h3>🎯 Final AI Prompt:</h3>
      <p id="finalPrompt"></p>
      <p class="tip" id="promptTip"></p>
    </div>

    <p class="footer">© 2025 Remote Sell Essence. All rights reserved.</p>
  </div>

  <script>
    function generatePrompt() {
      const goal = document.getElementById('goal').value.trim();
      let role = document.getElementById('role').value;
      const customRole = document.getElementById('customRole').value.trim();
      const tone = document.getElementById('tone').value;
      const format = document.getElementById('format').value;
      if (role === 'custom' && customRole) role = customRole;

      if (!goal) {
        alert('Please enter your goal or topic.');
        return;
      }

      const prompt = `Act as a ${role}. Your task is to ${goal}. 
Write the response in a ${tone} tone and present it as a ${format}. 
Include clear structure, actionable insights, and creativity.`;

      const tip = `💡 Tip: Add more context (target audience, length, or platform) for even better results.`;

      document.getElementById('finalPrompt').textContent = prompt;
      document.getElementById('promptTip').textContent = tip;
      document.getElementById('output').style.display = 'block';
    }

    function copyPrompt() {
      const text = document.getElementById('finalPrompt').textContent;
      navigator.clipboard.writeText(text);
      const btn = document.querySelector('.copy-btn');
      btn.textContent = 'Copied!';
      setTimeout(() => btn.textContent = 'Copy', 1500);
    }
  </script>
</body>
</html>
